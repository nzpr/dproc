package dproc

///** Bootstrap retrieves initial state to follow the network (build the state / validate / propose). */
//final case class Boot[F[_], Net, S, B, T](stRef: Ref[F, ST], pipe: Net => F[DProc.ST])
//
//final object Boot {
//  final case class ST()
//}
